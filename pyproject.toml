[project]
name = "aqi-checker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn==0.52.4",
    "requests==2.34.2",
    "python-dotenv==1.2.3",
    "psycopg[binary]==3.3.4",
]

[tool.vercel]
entrypoint = "backend.main:app"
